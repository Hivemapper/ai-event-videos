# Agent Removal

The legacy `/?agent=true` natural-language filter agent has been removed.

The home page always renders the event gallery. There is no Agent navigation item, no `/api/agent` endpoint, and no client-side agent chat UI. Anthropic configuration remains available for event video scene analysis and actor detection.
